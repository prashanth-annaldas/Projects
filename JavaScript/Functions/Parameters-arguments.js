var a = function (p1,p2){ // These are parameters, which gets those values are called parameters
    console.log(p1,p2);
}
a(10,20); // These are arguments, which pass the values inisde a function is called arguments